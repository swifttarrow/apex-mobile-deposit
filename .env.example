# Checkstream Environment Variables

# SQLite database path (default: checkstream.db)
DATABASE_URL=checkstream.db

# Server port (default: 8080)
PORT=8080

# Session signing key for operator/investor cookies (32 bytes recommended).
# Defaults to a dev secret if unset; set in production.
# SESSION_SECRET=your-32-byte-secret-here
